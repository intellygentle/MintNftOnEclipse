import { TypedExtension } from '.';
import { Delegate, DelegateRole } from '../generated';
export declare const manager: (address: Delegate['address'], roles: Delegate['roles'] | DelegateRole) => TypedExtension;
