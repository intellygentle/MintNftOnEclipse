import { Context, PublicKey, TransactionBuilder } from '@metaplex-foundation/umi';
import { LockInstructionAccounts } from './generated/instructions/lock';
export declare function lock(context: Pick<Context, 'identity' | 'programs'>, input: LockInstructionAccounts & {
    proxy?: PublicKey;
}): TransactionBuilder;
