import { Context, PublicKey, TransactionBuilder } from '@metaplex-foundation/umi';
import { ResizeInstructionAccounts, ResizeInstructionArgs } from './generated/instructions/resize';
export declare function resize(context: Pick<Context, 'programs' | 'identity' | 'payer'>, input: ResizeInstructionAccounts & ResizeInstructionArgs & {
    proxy?: PublicKey;
}): TransactionBuilder;
