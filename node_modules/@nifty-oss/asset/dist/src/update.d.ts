import { Context, PublicKey, TransactionBuilder } from '@metaplex-foundation/umi';
import { TypedExtension } from './extensions';
import { UpdateInstructionAccounts, UpdateInstructionArgs } from './generated/instructions/update';
export declare function update(context: Pick<Context, 'eddsa' | 'identity' | 'payer' | 'programs' | 'transactions'>, input: UpdateInstructionAccounts & Omit<UpdateInstructionArgs, 'extension'> & {
    extension?: TypedExtension;
} & {
    proxy?: PublicKey;
}): TransactionBuilder;
