import { Context, PublicKey, TransactionBuilderGroup } from '@metaplex-foundation/umi';
import { TypedExtension } from './extensions';
import { UpdateInstructionAccounts, UpdateInstructionArgs } from './generated/instructions/update';
export declare function updateWithBuffer(context: Pick<Context, 'eddsa' | 'identity' | 'payer' | 'programs' | 'transactions'>, input: UpdateInstructionAccounts & Omit<UpdateInstructionArgs, 'extension'> & {
    extension: TypedExtension;
    chunkSize?: number;
} & {
    proxy?: PublicKey;
}): TransactionBuilderGroup;
