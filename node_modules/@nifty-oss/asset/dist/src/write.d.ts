import { Context, Pda, PublicKey, Signer, TransactionBuilderGroup } from '@metaplex-foundation/umi';
export declare const DEFAULT_CHUNK_SIZE = 850;
export type WriteInstruction = {
    /** Address to derive the PDA from */
    asset: Signer;
    /** The account paying for the storage fees */
    payer?: Signer;
    /** The system program */
    systemProgram?: PublicKey | Pda;
    /** Data to write to the buffer. */
    data: Uint8Array;
    /**
     * The size of each chunk to write to the buffer.
     * @default `850`
     */
    chunkSize?: number;
};
export declare const write: (context: Pick<Context, 'eddsa' | 'identity' | 'payer' | 'programs' | 'transactions'>, input: WriteInstruction) => TransactionBuilderGroup;
